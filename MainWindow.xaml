<Window x:Class="TestCandidat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:local="clr-namespace:TestCandidat"
        xmlns:localBase="clr-namespace:TestCandidat;assembly=TestCandidatBase"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" x:Name="Me">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="VisibleIfTrueConverter" />
    </Window.Resources>
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <StackPanel>
        <Popup IsOpen="{Binding PopupVisible, Mode=OneWay}"
               Visibility="Visible"
               Placement="Center"
               PlacementTarget="{Binding ElementName=Me}" AllowsTransparency="True">
            <Border Background="LightBlue" Width="200" Height="50" CornerRadius="20" BorderBrush="Black" BorderThickness="1">
                <TextBlock DataContext="{Binding Spectacular}" TextWrapping="Wrap" Text="{Binding Text, NotifyOnSourceUpdated=True}" HorizontalAlignment="Stretch" VerticalAlignment="Center" TextAlignment="Center"/>
            </Border>
   </Popup>
    <localBase:SpectacularControl DataContext="{Binding Spectacular}"/>
    </StackPanel>
</Window>
